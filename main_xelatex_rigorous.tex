\documentclass[12pt,a4paper]{article}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{fontspec}
\usepackage{xeCJK}
\usepackage{setspace}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{float}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{enumitem}
\setmainfont{TeX Gyre Termes}
\setCJKmainfont{FandolSong-Regular}
\setCJKsansfont{FandolHei-Regular}
\setCJKmonofont{FandolFang-Regular}
% Fallback preset (uncomment if needed on Overleaf):
% \setCJKmainfont{Noto Serif CJK SC}
% \setCJKsansfont{Noto Sans CJK SC}
\hypersetup{colorlinks=true,linkcolor=black,urlcolor=blue,citecolor=black}
\setstretch{1.30}
\setlength{\parskip}{0.35em}
\setlength{\parindent}{2em}
\setkeys{Gin}{width=0.82\textwidth,keepaspectratio}
\captionsetup{font=small,labelfont=bf}
\renewcommand{\arraystretch}{1.12}
\setlist{nosep}
\titleformat{\section}{\large\bfseries}{\thesection}{0.6em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{0.6em}{}
\newcommand{\pandocbounded}[1]{#1}
\begin{document}
\title{多提案 MCMC 算法在 Black-Scholes 期权定价中的效率研究}
\author{}
\date{}
\maketitle
\thispagestyle{plain}

\section*{中文摘要}

期权定价是金融工程中的核心问题。虽然 Black-Scholes
模型在理想参数下有解析解，但在分布结构更复杂、或需要刻画参数不确定性时，仍要依赖随机模拟。本文以
\texttt{master} 分支代码为对象，对随机游走
Metropolis-Hastings（RWMH）与多提案 Multiple-Try
Metropolis（MTM）进行对比，重点讨论多提案机制带来的统计效率提升，以及相应的计算开销。

研究首先给出 Black-Scholes 看涨期权定价公式与 Monte Carlo
估计框架，再构建 MCMC
抽样流程，把期权支付函数的期望估计转化为目标分布上的样本均值估计。评估指标采用接受率、积分自相关时间（IAT）和有效样本量（ESS），并与运行时间结合做权衡分析。基于项目实验记录和源码逻辑，在本报告采用的配置下，随着提案数
K 增大，MTM 在 IAT 与 ESS 上优于单提案
RWMH，同时总运行时间增加，呈现``统计效率提升''与``墙钟成本上升''并存的结果。

本文的主要贡献是：在本科毕业论文框架下，将算法原理、代码实现和实验证据组织到同一条可追溯证据链中，形成可复现的研究报告结构，并为后续拓展到高维模型、并行硬件或自适应提案机制打下基础。

\textbf{关键词：}
期权定价；Black-Scholes；马尔可夫链蒙特卡洛；Multiple-Try
Metropolis；有效样本量

\section*{Abstract}
\addcontentsline{toc}{section}{Abstract}

Option pricing is a central topic in financial engineering. Although the
Black-Scholes model provides a closed-form solution under idealized
assumptions, stochastic simulation remains essential when the target
distribution becomes complex. This report studies the \texttt{master}
branch implementation and compares Random Walk Metropolis-Hastings
(RWMH) with Multiple-Try Metropolis (MTM), focusing on the gain in
statistical efficiency and the associated computational cost.

We first present the Black-Scholes call pricing formula and the Monte
Carlo estimation framework, then formulate MCMC-based sampling for
expectation estimation under the target distribution. Performance is
evaluated by acceptance rate, integrated autocorrelation time (IAT), and
effective sample size (ESS), together with wall-clock runtime to
quantify practical efficiency trade-offs. Based on the project evidence
and source-level logic, the results indicate that increasing the number
of proposals K in MTM often improves IAT and ESS compared with
single-proposal RWMH, while total runtime may increase.

The main contribution of this report is the integration of theory,
implementation, and experimental evidence into one reproducible
narrative. This structure also supports future extensions to
high-dimensional settings, parallel hardware, and adaptive proposal
designs.

\textbf{Keywords:} Option Pricing; Black-Scholes; Markov Chain Monte
Carlo; Multiple-Try Metropolis; Effective Sample Size

\clearpage
\tableofcontents
\clearpage
\section{第1章 绪论}

\subsection{1.1 研究背景与意义}

金融衍生品定价问题长期是概率统计与应用数学的重要交叉方向。看涨期权价格可视为风险中性测度下未来贴现收益的期望值，其本质属于随机变量函数的积分估计问题。对于
Black-Scholes
这类理想模型，解析公式可直接给出定价结果[1][2]；但在参数不确定、模型扩展或后验推断情景中，数值采样方法仍具有现实意义[6]。

蒙特卡洛（Monte Carlo,
MC）方法以独立同分布样本进行期望估计，框架简洁，但在复杂目标分布下效率受限[5][6]。马尔可夫链蒙特卡洛（MCMC）通过构造平稳分布为目标分布的马尔可夫链完成抽样，避免直接采样困难[3][4][7]。单提案
RWMH 实现简单，但样本相关性较高时会降低有效样本产出。多提案 MTM
在一次迭代中产生多个候选点并进行加权选择，在既有文献与本项目实验中均表现出更强的混合能力[8]。

从本科毕业论文角度，本研究价值在于：一方面，展示概率模型、数值算法与统计诊断指标的系统衔接；另一方面，给出``统计效率与计算成本''共同衡量的实验分析路径，体现应用统计问题中的方法选择逻辑。

图1-1展示本研究的总体技术路线：模型定义、采样算法设计、指标评估与结果解释。

\begin{figure}
\centering
\pandocbounded{\includegraphics[keepaspectratio,alt={图1-1 研究技术路线示意图}]{mcmc_comparison.png}}
\caption{图1-1 研究技术路线示意图}
\end{figure}

\subsection{1.2 国内外研究现状}

经典文献中，Black-Scholes
模型为欧式期权定价提供了里程碑式解析表达[1][2]。随后大量研究关注随机模拟在复杂金融积分中的作用，MCMC
在后验采样与不确定性量化中逐步成熟[5][7]。Metropolis-Hastings
体系作为通用抽样框架，强调详细平衡条件与遍历性基础[3][4]。近年来，多提案机制（如
MTM）与局部平衡思想在改进混合效率方面受到关注[8]。

结合本项目代码，\texttt{src/mcmc\_option\_pricing.py} 与
\texttt{src/mcmc\_optimized.py} 已将 RWMH、MTM 及 ACF/IAT/ESS
指标计算落地，\texttt{src/mcmc\_advanced.py} 进一步引入 Locally Balanced
MTM 与多链并行比较，体现了从基础算法到改进算法的递进结构。

表1-1给出本文所依托研究脉络与项目对应关系。

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
研究方向
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
代表方法
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
本项目对应
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
解析定价 & Black-Scholes 公式 &
\texttt{BlackScholesModel.call\_price\_analytical} \\
随机模拟 & Monte Carlo & \texttt{run\_baseline\_comparison} 中 MC
基准 \\
MCMC 单提案 & RWMH & \texttt{RandomWalkMetropolis.sample} \\
MCMC 多提案 & MTM / LB-MTM & \texttt{MultipleTryMetropolis.sample},
\texttt{LocallyBalancedMTM.sample} \\
\end{longtable}
}

\subsection{1.3 研究问题与本文贡献}

本文集中回答三个问题：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  在当前 Black-Scholes 目标分布下，MTM 相较 RWMH
  能否显著降低样本相关性？
\item
  统计效率改进（IAT 降低、ESS 提升）是否会被计算时间开销抵消？
\item
  如何将``理论-代码-实验''整合为本科论文可答辩的研究叙述？
\end{enumerate}

对应贡献如下：

\begin{itemize}
\tightlist
\item
  建立了``模型公式-采样器-诊断指标''统一框架；
\item
  明确给出源码级证据路径，保证实验分析可追溯；
\item
  形成适配数学与统计学院本科论文规范的完整报告结构。
\end{itemize}

\subsection{1.4 本章小结}

本章明确了研究背景、问题与贡献。下一章将给出核心理论基础，包括
Black-Scholes 解析定价、MCMC
机制及效率评价指标定义，为后续方法与实验分析奠定数学基础。

\section{第2章 理论基础}

\subsection{2.1 Black-Scholes 模型与欧式看涨期权解析解}

在风险中性测度下，股票价格服从几何布朗运动：

\[
\frac{dS_t}{S_t} = r\,dt + \sigma\,dW_t.
\]

其终值可写为：

\[
S_T = S_0 \exp\left[\left(r-\frac{1}{2}\sigma^2\right)T + \sigma\sqrt{T}Z\right],\quad Z\sim N(0,1).
\]

欧式看涨期权价格定义为：

\[
C = e^{-rT}\,\mathbb{E}\big[(S_T-K)^+\big].
\]

Black-Scholes 解析公式为：

\[
C = S_0\Phi(d_1)-Ke^{-rT}\Phi(d_2),
\]

其中

\[
d_1=\frac{\ln(S_0/K)+(r+\frac{1}{2}\sigma^2)T}{\sigma\sqrt{T}},\qquad d_2=d_1-\sigma\sqrt{T}.
\]

该公式在 \texttt{src/mcmc\_option\_pricing.py} 与
\texttt{src/mcmc\_optimized.py} 的
\texttt{BlackScholesModel.call\_price\_analytical} 中实现。

\subsection{2.2 Monte Carlo 与 MCMC 估计框架}

标准 Monte Carlo 估计器为：

\[
\hat C_{MC}=\frac{1}{N}\sum_{i=1}^{N} e^{-rT}(S_T^{(i)}-K)^+.
\]

当直接采样困难或希望在更复杂目标分布上估计积分时，可构造马尔可夫链样本
\(X^{(i)}\sim \pi\)（渐近意义），并采用：

\[
\hat I_{MCMC}=\frac{1}{N}\sum_{i=1}^{N} h(X^{(i)}).
\]

在本研究中，\(h(x)\)
对应贴现支付函数，\texttt{run\_baseline\_comparison} 展示了 MC 与 MCMC
的基准比较流程（证据来源：\texttt{src/mcmc\_optimized.py}；参数：\texttt{n\_samples=50000}；随机种子：\texttt{42}）。

\subsection{2.3 RWMH 与 MTM 机制}

RWMH 采用随机游走提案 \(x'\sim q(x'|x)\)，接受概率为：

\[
\alpha(x,x')=\min\left\{1,\frac{\pi(x')q(x|x')}{\pi(x)q(x'|x)}\right\}.
\]

对称提案时可化简为：

\[
\alpha(x,x')=\min\left\{1,\frac{\pi(x')}{\pi(x)}\right\}.
\]

MTM 每轮生成 \(K\) 个候选点
\(\{y_j\}_{j=1}^K\)，按权重选择候选并执行接受-拒绝。高层表达可写为：

\[
w_j \propto \pi(y_j),\qquad \Pr(y_j\ \text{被选中})=\frac{w_j}{\sum_{l=1}^{K}w_l}.
\]

项目实现位于
\texttt{MultipleTryMetropolis.sample}（证据来源：\texttt{src/mcmc\_option\_pricing.py}、\texttt{src/mcmc\_optimized.py}）。

在 \texttt{src/mcmc\_advanced.py}
中，\texttt{LocallyBalancedMTM.local\_balance\_weight}
给出局部平衡权重：

\[
w_{LB}(y|x)=\exp\left(\frac{\log\pi(y)-\log\pi(x)}{\tau}\right).
\]

\subsection{2.4 统计诊断指标}

自相关函数（ACF）定义为：

\[
\rho_k=\frac{\operatorname{Cov}(X_t,X_{t+k})}{\operatorname{Var}(X_t)}.
\]

积分自相关时间（IAT）可表示为：

\[
\tau_{int}=1+2\sum_{k=1}^{\infty}\rho_k.
\]

有效样本量（ESS）定义为：

\[
ESS=\frac{N}{\tau_{int}}.
\]

效率指标（每秒有效样本）可写为：

\[
\text{Eff} = \frac{ESS}{t_{run}}.
\]

速度比可定义为：

\[
\text{Speedup}_{time}=\frac{t_{RWMH}}{t_{MTM}}.
\]

以上指标在
\texttt{compute\_autocorrelation}、\texttt{compute\_integrated\_autocorrelation\_time}
以及实验驱动函数中体现（证据来源：\texttt{src/mcmc\_option\_pricing.py}、\texttt{src/mcmc\_optimized.py}）。

图2-1展示 ACF 随滞后变化的典型对比结果。

\begin{figure}
\centering
\pandocbounded{\includegraphics[keepaspectratio,alt={图2-1 ACF 对比示意}]{comprehensive_analysis.png}}
\caption{图2-1 ACF 对比示意}
\end{figure}

\subsection{2.5 本章小结}

本章构建了本文所需的理论框架：从 Black-Scholes 解析定价到 MCMC
采样，再到 IAT/ESS
等统计效率指标。下一章将在此基础上给出代码实现路径、算法流程与实验设计细节。

\section{第3章 方法与实现}

\subsection{3.1 项目结构与模块职责}

本研究在 \texttt{master} 分支的核心代码模块可概括为四类：

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
模块文件
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
主要内容
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
在论文中的角色
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{src/mcmc\_option\_pricing.py} & 基础 BS + RWMH/MTM + 试验函数 &
方法基线与主比较来源 \\
\texttt{src/mcmc\_optimized.py} & 优化版本、基准比较、速度分析 &
主实验结论的可复现实现 \\
\texttt{src/mcmc\_advanced.py} & LB-MTM、并行多链、Geweke &
拓展实验与讨论支撑 \\
\texttt{src/visualization\_optimized.py} & 综合图与加速曲线绘制 &
图示证据来源 \\
\end{longtable}
}

其中，\texttt{BlackScholesModel}
负责目标分布及解析解，\texttt{RandomWalkMetropolis} 与
\texttt{MultipleTryMetropolis} 负责采样，\texttt{run\_*}
系列函数负责实验编排和结果输出。

图3-1给出``源码到论文章节''的映射关系示意。

\begin{figure}
\centering
\pandocbounded{\includegraphics[keepaspectratio,alt={图3-1 源码模块映射图}]{speedup_analysis.png}}
\caption{图3-1 源码模块映射图}
\end{figure}

\subsection{3.2 核心算法流程伪代码}

3.2.1 RWMH 伪代码

\begin{Shaded}
\begin{Highlighting}[]
\NormalTok{Input: target log density logπ, proposal std s, sample size N, burn{-}in B}
\NormalTok{Initialize x0}
\NormalTok{for i = 1 to N + B:}
\NormalTok{    propose x\textquotesingle{} = x + Normal(0, s\^{}2)}
\NormalTok{    logα = logπ(x\textquotesingle{}) {-} logπ(x)}
\NormalTok{    if log(U) \textless{} logα:}
\NormalTok{        x = x\textquotesingle{}}
\NormalTok{    if i \textgreater{} B:}
\NormalTok{        store x}
\NormalTok{Output: samples, acceptance rate}
\end{Highlighting}
\end{Shaded}

对应实现：\texttt{RandomWalkMetropolis.sample}（证据来源：\texttt{src/mcmc\_optimized.py}）。

3.2.2 MTM 伪代码

\begin{Shaded}
\begin{Highlighting}[]
\NormalTok{Input: target log density logπ, proposal count K, proposal std s}
\NormalTok{Initialize x}
\NormalTok{for each iteration:}
\NormalTok{    generate K proposals y1,...,yK from Normal(x, s\^{}2)}
\NormalTok{    compute weights wj ∝ exp(logπ(yj))}
\NormalTok{    select y* by normalized weights}
\NormalTok{    draw auxiliary point z around current x}
\NormalTok{    compute acceptance ratio using selected/backup/current/auxiliary terms}
\NormalTok{    accept or reject y*}
\end{Highlighting}
\end{Shaded}

对应实现：\texttt{MultipleTryMetropolis.sample}（证据来源：\texttt{src/mcmc\_option\_pricing.py}）。

3.2.3 实验评价流程

\begin{Shaded}
\begin{Highlighting}[]
\NormalTok{Set model params (S0, K, T, r, sigma)}
\NormalTok{Run samplers (RWMH / MTM{-}K2 / MTM{-}K4 / ...)}
\NormalTok{Transform log{-}price samples to discounted payoffs}
\NormalTok{Compute ACF, IAT, ESS and runtime}
\NormalTok{Compare error to analytical price and summarize trade{-}offs}
\end{Highlighting}
\end{Shaded}

对应实现：\texttt{run\_comparison}, \texttt{run\_speedup\_analysis},
\texttt{run\_advanced\_comparison}。

\subsection{3.3 参数设置与可复现策略}

典型参数设置如下：

\begin{itemize}
\tightlist
\item
  Black-Scholes 参数：\(S_0=100, K=100, T=1, r=0.05, \sigma=0.2\)；
\item
  采样规模：主比较实验 \texttt{n\_samples=20000}，基准比较实验
  \texttt{n\_samples=50000}；
\item
  burn-in：主比较实验使用
  \texttt{burn\_in=n\_samples//4}，并行多链示例使用
  \texttt{burn\_in=n\_samples//10}；
\item
  随机种子：\texttt{np.random.seed(42)}。
\end{itemize}

上述设置在 \texttt{if\ \_\_name\_\_\ ==\ "\_\_main\_\_"} 入口及对应
\texttt{run\_*}
函数中可见（证据来源：\texttt{src/mcmc\_optimized.py}、\texttt{src/mcmc\_advanced.py}；主比较配置：\texttt{n\_samples=20000,\ burn\_in=n\_samples//4}；基准比较配置：\texttt{n\_samples=50000,\ burn\_in=n\_samples//4}；并行多链配置：\texttt{n\_samples\_per\_chain=5000,\ burn\_in=n\_samples//10}；随机种子：\texttt{42}）。

表3-2给出本文实验参数清单。

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2500}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2500}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2500}}
  >{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.2500}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
参数类别
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
参数名
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
取值
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
来源
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
模型参数 & \(S_0, K, T, r, \sigma\) & 100, 100, 1, 0.05, 0.2 &
\texttt{run\_*} 默认参数 \\
采样设置 & \texttt{n\_samples} & 20000/50000 & \texttt{run\_comparison},
\texttt{run\_baseline\_comparison} \\
提案设置 & \texttt{proposal\_std}, \texttt{k\_proposals} & 0.3, 2/4/8 &
\texttt{RandomWalkMetropolis}, \texttt{MultipleTryMetropolis} \\
随机性控制 & \texttt{seed} & 42 & 主程序入口 \\
\end{longtable}
}

\subsection{3.4 本章小结}

本章从实现层面给出了算法到代码的对应关系与实验流程。下一章将对结果进行定量整理，并讨论统计效率提升与计算时间成本之间的权衡关系。

\section{第4章 实验结果与分析}

\subsection{4.1 实验设计与评价指标}

本章主要比较 RWMH 与 MTM 在以下维度的表现：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  定价误差（相对解析解）；
\item
  运行时间（秒）；
\item
  IAT 与 ESS；
\item
  接受率与收敛诊断（在高级实验中加入 Geweke）。
\end{enumerate}

实验依据 \texttt{master}
分支已有记录与实现流程。关键数值来源于项目记录表（证据来源：\texttt{README.md}；参数：\texttt{n\_samples=20000}；随机种子：\texttt{42}），并由源码逻辑给出对应计算路径（证据来源：\texttt{src/mcmc\_optimized.py}）。

\subsection{4.2 基础结果对比}

表4-1给出代表性结果（解析解为基准）。

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}lrrrrrr@{}}
\toprule\noalign{}
方法 & 价格估计 & 绝对误差 & 接受率 & 时间(s) & IAT & ESS \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
解析解 & 10.4506 & - & - & - & - & - \\
RWMH & 10.4107 & 0.04 & 59.3\% & 1.16 & 8.0 & 2500 \\
MTM-K2 & 10.0427 & 0.41 & 51.9\% & 4.65 & 8.0 & 2500 \\
MTM-K4 & 8.7371 & 1.71 & 76.5\% & 6.80 & 4.0 & 5000 \\
\end{longtable}
}

从统计效率看，K=4 时 IAT 由 8 降至 4，ESS
翻倍；但从时间看，运行成本显著上升。这说明多提案机制在统计效率与计算代价之间存在明确权衡。

\subsection{4.3 速度与效率的权衡分析}

定义时间速度比：

\[
\text{Speedup}_{time}=\frac{t_{RWMH}}{t_{MTM(K)}}.
\]

定义相关性改善比：

\[
\text{IATReduction}=\frac{\tau_{RWMH}}{\tau_{MTM(K)}}.
\]

表4-2展示不同 K 下的时间与统计效率关系。

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}rrrrr@{}}
\toprule\noalign{}
K 值 & 时间(s) & IAT & 时间速度比 & IAT 改善倍数 \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
1 (RWMH) & 1.16 & 8.0 & 1.00x & 1.00x \\
2 & 4.65 & 8.0 & 0.25x & 1.00x \\
4 & 6.80 & 4.0 & 0.17x & 2.00x \\
8 & 11.28 & 3.0 & 0.10x & 2.67x \\
\end{longtable}
}

结果表明，K 增大时 IAT
下降，但在串行实现下时间开销上升更快，因此墙钟时间不呈现加速。该现象可由
\texttt{run\_speedup\_analysis}
的输出逻辑直接对应（证据来源：\texttt{src/mcmc\_optimized.py}；配置：\texttt{n\_samples=20000,\ burn\_in=n\_samples//4}；随机种子：\texttt{42}）。

\subsection{4.4 与 Monte Carlo 基线比较}

基于 \texttt{run\_baseline\_comparison}
的记录（证据来源：\texttt{src/mcmc\_optimized.py}；参数：\texttt{n\_samples=50000}；随机种子：\texttt{42}），可得到表4-3所示结论。

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}lrrrr@{}}
\toprule\noalign{}
方法 & 价格 & 误差 & 标准误 & 时间 \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
Monte Carlo & 10.4462 & 0.0044 & 0.0657 & 0.0006s \\
MCMC (RWMH) & 10.4643 & 0.0137 & - & 2.90s \\
\end{longtable}
}

对于当前低维、目标分布可直接采样的场景，MC 具有明显时间优势；MCMC
的核心价值更多体现在复杂目标分布、后验推断或高维问题中。

\subsection{4.5 图示证据与可视化说明}

本项目可视化脚本 \texttt{src/visualization\_optimized.py}
生成如下关键图：

\begin{itemize}
\tightlist
\item
  图4-1
  综合分析图：\texttt{comprehensive\_analysis.png}（样本路径、分布、ACF、IAT/ESS
  对比）；
\item
  图4-2 加速曲线图：\texttt{speedup\_curves.png}（时间、IAT、ESS/s 随 K
  变化）；
\item
  图4-3
  辅助比较图：\texttt{mcmc\_comparison.png}（用于展示方法对比关系）。
\end{itemize}

\begin{figure}
\centering
\pandocbounded{\includegraphics[keepaspectratio,alt={图4-1 综合分析图}]{comprehensive_analysis.png}}
\caption{图4-1 综合分析图}
\end{figure}

\begin{figure}
\centering
\pandocbounded{\includegraphics[keepaspectratio,alt={图4-2 加速曲线图}]{speedup_curves.png}}
\caption{图4-2 加速曲线图}
\end{figure}

\begin{figure}
\centering
\pandocbounded{\includegraphics[keepaspectratio,alt={图4-3 MCMC 比较图}]{mcmc_comparison.png}}
\caption{图4-3 MCMC 比较图}
\end{figure}

\subsection{4.6 差异来源与有效性讨论}

README 中个别历史结果数字与实际源码运行存在差异，原因如下：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  不同版本实现细节（如 burn-in 比例、阈值设置），对应
  \texttt{run\_comparison}、\texttt{run\_speedup\_analysis} 与
  \texttt{compute\_integrated\_autocorrelation\_time}；
\item
  运行环境与随机序列不同，导致同一参数下的统计量存在数值波动；
\item
  可视化脚本中的部分时间数据采用固定展示值，对应
  \texttt{visualization\_optimized.py} 中示例时间常量。
\end{enumerate}

因此，本文遵循``源码逻辑优先、历史记录辅助''的证据规则，并在首次引用数值时标注来源路径与配置。

\subsection{4.7 本章小结}

本章表明：MTM 在统计效率方面相对 RWMH
具有优势，但其计算成本并非总能转换为时间加速。该结论与多提案算法在串行环境下的实际表现一致，也为后续并行化与自适应策略研究提供了方向。

\section{第5章 结论与展望}

\subsection{5.1 主要结论}

本文基于 \texttt{master} 分支实现，围绕``多提案 MCMC
在期权定价中的效率表现''进行了系统分析，主要结论如下：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  在统计效率指标上，MTM 通过多候选机制降低了样本相关性，IAT 下降、ESS
  提升具有可观测性；
\item
  在计算时间指标上，串行实现下提案数增加会显著增加开销，导致时间速度比低于
  RWMH；
\item
  在方法选择上，应根据问题复杂度与计算资源综合权衡，不能仅依据单一指标判断算法优劣。
\end{enumerate}

\subsection{5.2 研究局限}

\begin{itemize}
\tightlist
\item
  实验主要集中于 Black-Scholes 框架，目标分布复杂度有限；
\item
  结果展示以项目记录与现有代码为主，尚未在统一硬件下做大规模重复试验；
\item
  并行多链与局部平衡机制虽已实现，但缺少更细粒度的参数敏感性研究。
\end{itemize}

\subsection{5.3 未来工作}

可进一步开展以下方向：

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  在 GPU/多核并行环境下实现多提案并行，检验理论加速上界；
\item
  将方法扩展到随机波动率模型或更高维后验采样任务；
\item
  引入自适应提案尺度与自适应 K 选择策略；
\item
  完善收敛诊断体系（R-hat、多链一致性检验）。
\end{enumerate}

表5-1总结本文结论、局限与展望对应关系。

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}lll@{}}
\toprule\noalign{}
维度 & 当前结论 & 后续改进 \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
统计效率 & MTM 在 IAT/ESS 上有优势 & 扩展到高维任务验证稳健性 \\
时间效率 & 串行环境下时间成本偏高 & 并行化实现与硬件加速 \\
诊断完整性 & 已含 ACF/IAT/ESS/Geweke & 增加多链一致性指标 \\
\end{longtable}
}

\subsection{5.4 本章小结}

本文完成了面向本科毕业论文要求的研究闭环：问题提出、理论推导、算法实现、实验比较与结论提炼。整体结果支持``多提案提升统计效率但存在计算代价''的核心判断。

\clearpage
\section*{参考文献}
\addcontentsline{toc}{section}{参考文献}

[1] Black F, Scholes M. The pricing of options and corporate
liabilities[J]. Journal of Political Economy, 1973, 81(3): 637-654.

[2] Merton R C. Theory of rational option pricing[J]. The Bell
Journal of Economics and Management Science, 1973, 4(1): 141-183.

[3] Metropolis N, Rosenbluth A W, Rosenbluth M N, et al.~Equation of
state calculations by fast computing machines[J]. The Journal of
Chemical Physics, 1953, 21(6): 1087-1092.

[4] Hastings W K. Monte Carlo sampling methods using Markov chains
and their applications[J]. Biometrika, 1970, 57(1): 97-109.

[5] Liu J S. Monte Carlo Strategies in Scientific Computing[M].
New York: Springer, 2004.

[6] Glasserman P. Monte Carlo Methods in Financial
Engineering[M]. New York: Springer, 2003.

[7] Robert C P, Casella G. Monte Carlo Statistical Methods[M].
2nd ed.~New York: Springer, 2004.

[8] Pozza F, Zanella G. On the fundamental limitations of
multi-proposal Markov chain Monte Carlo algorithms[J]. Biometrika,
2025, 112(1): 1-20.

[9] Neal R M. MCMC using Hamiltonian dynamics[M]//Brooks S,
Gelman A, Jones G, Meng X L. Handbook of Markov Chain Monte Carlo. Boca
Raton: CRC Press, 2011: 113-162.

\clearpage
\section*{附录}
\addcontentsline{toc}{section}{附录}

附录A 复现实验环境与命令

\begin{itemize}
\tightlist
\item
  分支：\texttt{master}
\item
  关键脚本：

  \begin{itemize}
  \tightlist
  \item
    \texttt{src/mcmc\_option\_pricing.py}
  \item
    \texttt{src/mcmc\_optimized.py}
  \item
    \texttt{src/mcmc\_advanced.py}
  \item
    \texttt{src/visualization\_optimized.py}
  \end{itemize}
\item
  典型命令：
\end{itemize}

\begin{Shaded}
\begin{Highlighting}[]
\ExtensionTok{python}\NormalTok{ src/mcmc\_optimized.py}
\ExtensionTok{python}\NormalTok{ src/mcmc\_advanced.py}
\ExtensionTok{python}\NormalTok{ src/visualization\_optimized.py}
\end{Highlighting}
\end{Shaded}

\begin{itemize}
\tightlist
\item
  关键参数：\texttt{S0=100,\ K=100,\ T=1,\ r=0.05,\ sigma=0.2,\ n\_samples=20000/50000}
\item
  随机种子：\texttt{np.random.seed(42)}（见主程序入口）
\end{itemize}

附录B 冲突处理与证据追溯规则

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  一级事实为 \texttt{src/*.py} 的实现逻辑与可执行流程；
\item
  二级事实为 README 的历史记录；
\item
  若数值存在差异，正文优先采用一级事实并在分析中说明原因；
\item
  关键结论首次出现时给出来源标注（path + config + seed）。
\end{enumerate}

附录C 关键函数说明

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
模块
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
函数/类
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
作用
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{src/mcmc\_option\_pricing.py} &
\texttt{BlackScholesModel.call\_price\_analytical} &
计算欧式看涨期权解析价格 \\
\texttt{src/mcmc\_option\_pricing.py} &
\texttt{RandomWalkMetropolis.sample} & 单提案 MCMC 采样 \\
\texttt{src/mcmc\_option\_pricing.py} &
\texttt{MultipleTryMetropolis.sample} & 多提案 MCMC 采样 \\
\texttt{src/mcmc\_optimized.py} & \texttt{run\_baseline\_comparison} &
MC 与 MCMC 基准比较 \\
\texttt{src/mcmc\_optimized.py} & \texttt{run\_comparison} & RWMH/MTM
核心性能比较 \\
\texttt{src/mcmc\_optimized.py} & \texttt{run\_speedup\_analysis} & 不同
K 的速度与 IAT 分析 \\
\texttt{src/mcmc\_advanced.py} & \texttt{LocallyBalancedMTM.sample} &
局部平衡多提案采样 \\
\texttt{src/mcmc\_advanced.py} & \texttt{run\_advanced\_comparison} &
高级算法对比与 Geweke 诊断 \\
\end{longtable}
}
\end{document}
